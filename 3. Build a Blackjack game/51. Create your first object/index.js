// Create an object that represents an airbnb castle listing.
// It should contain at least one boolean, one string, one number, and one array
// Log out at least two of the keys using the dot notation

let castle = {
    isSuperhost: true,
    location: "Private room in Galway",
    reviews: 1268,
    keyLocationAttributes: ["4 guests", "1 bedroom", "2 beds", "Private half-bath"]
}

console.log(castle.location);
console.log(castle.superhost);
console.log(castle.reviews);
console.log(castle.keyLocationAttributes[0]);


